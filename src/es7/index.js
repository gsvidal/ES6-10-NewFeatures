//Includes method
let numbers = [1,2,5,6,77,200];
console.log(numbers.includes(6) ? "Si se encuentra el valor" : "No se encuentra el valor");

//Exponential
let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);